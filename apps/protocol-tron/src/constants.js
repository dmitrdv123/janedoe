const DEPLOYMENTS_FOLDER = '../installer/data/deployments'

const NATIVE_NAME='TRX'
const NATIVE_SYMBOL='TRX'
const NATIVE_DECIMALS=6

module.exports = {
  DEPLOYMENTS_FOLDER,
  NATIVE_NAME,
  NATIVE_SYMBOL,
  NATIVE_DECIMALS
}
